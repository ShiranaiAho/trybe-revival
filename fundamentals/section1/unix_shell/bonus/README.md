# Day 1 - Unix & Shell: Bonus

### Objectives:
- ✅ Install a program with `sudo apt-get install`
- ✅ Try `cmatrix`, `fortune`, `sl` and `cowsay`
- ✅ Try `factor` and `rev`
- ✅ See a movie with `telnet towel.blinkenlights.nl`